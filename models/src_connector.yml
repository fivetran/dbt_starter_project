# for source packages/pulling from the raw connector_THIS_WORKED data

version: 2

sources:
  - name: connector_THIS_WORKED_name 
    database: "{{ var ('connector_THIS_WORKED_database', target.database) }}"
    schema: "{{ var ('connector_THIS_WORKED_schema', 'connector_THIS_WORKED_name') }}"

    loader: fivetran
    loaded_at_field: _fivetran_synced
      
    freshness:
      warn_after: {count: 72, period: hour}
      error_after: {count: 96, period: hour}

    tables:
    # tables with table and column- level descriptions
         